/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment when Sanity CMS is connected to allow CMS-hosted images:
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: "https",
  //       hostname: "cdn.sanity.io",
  //     },
  //   ],
  // },
};

export default nextConfig;
